# Legacy

This directory contains frozen historical implementations that are outside the
supported markdown-formal product boundary. They are not part of default
builds, tests, releases, npm packages, or AI integration guidance.

- `vscode-extension/`: the former embedded VS Code preview implementation.

Use `markdown-formal serve <project>` and the local Reader for supported
reading workflows.
